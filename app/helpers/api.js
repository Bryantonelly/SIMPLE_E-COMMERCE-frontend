import { environment } from "./enviroment.js";

const   CATEGORIES  = `${environment.urlEndPoint}categories`,
        PRODUCTS    =  `${environment.urlEndPoint}products`

export default {
    CATEGORIES,
    PRODUCTS
}